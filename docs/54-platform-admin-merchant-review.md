# Platform Admin merchant review

The protected queue shows `PendingReview` registrations. Admins may approve, reject with reason, request correction with reason, suspend with reason, or reactivate. Every transition is transactional and recorded in the merchant audit log.

Approval requires verified email and phone. Review routes require Platform Admin authorization. There is no balance-editing route and onboarding causes no financial posting.
