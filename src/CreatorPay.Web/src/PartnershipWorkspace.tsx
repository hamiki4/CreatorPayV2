import {FormEvent,useEffect,useState} from 'react'
type Partnership={id:string;merchantName:string;creatorName:string;status:string;requestedAtUtc:string}
type Found={id:string;publicMerchantId?:string;tradingName?:string;publicCreatorId?:string;displayName?:string;email?:string;city?:string}
const token=()=>localStorage.getItem('creatorpay_access_token')??''
async function api<T>(path:string,init?:RequestInit):Promise<T>{const r=await fetch(path,{...init,headers:{'Content-Type':'application/json',Authorization:`Bearer ${token()}`}});if(!r.ok){const p=await r.json().catch(()=>({}));throw new Error(p.detail??`Request failed (${r.status}).`)}return r.json()}
export function PartnershipWorkspace({merchant}:{merchant:boolean}){const[search,setSearch]=useState(false),[q,setQ]=useState(''),[status,setStatus]=useState('All'),[items,setItems]=useState<Partnership[]|null>(null),[found,setFound]=useState<Found[]>([]),[message,setMessage]=useState('');const load=()=>api<Partnership[]>(`/api/v1/${merchant?'merchant':'creator'}/partnerships${status==='All'?'':`?status=${status}`}`).then(setItems).catch(e=>setMessage(e.message));useEffect(()=>{void load()},[merchant,status]);async function find(e:FormEvent){e.preventDefault();try{setFound(await api(`/api/v1/${merchant?'merchant/creators':'creator/merchants'}/search?q=${encodeURIComponent(q)}`))}catch(x){setMessage((x as Error).message)}}async function add(x:Found){if(merchant&&!confirm(`Approve ${x.displayName}?`))return;try{await api(merchant?'/api/v1/merchant/partnerships':'/api/v1/creator/partnerships/requests',{method:'POST',body:JSON.stringify(merchant?{creatorId:x.id,confirmApproval:true,locationIds:[]}:{merchantId:x.id,introductoryMessage:prompt('Introduction (optional):')||null})});setMessage(merchant?'Creator approved.':'Request sent.');load()}catch(e){setMessage((e as Error).message)}}async function action(x:Partnership,name:string){if(!confirm(`Confirm ${name.replace('-',' ')}?`))return;try{await api(`/api/v1/${merchant?'merchant':'creator'}/partnerships/${x.id}/${name}`,{method:'POST',body:JSON.stringify({reason:prompt('Reason (optional):')||null})});load()}catch(e){setMessage((e as Error).message)}}return <><nav><button className={!search?'active':''} onClick={()=>setSearch(false)}>Partnerships</button><button className={search?'active':''} onClick={()=>setSearch(true)}>Search {merchant?'creators':'merchants'}</button></nav>{message&&<aside>{message}</aside>}{search?<section><h2>Find {merchant?'creators':'merchants'}</h2><form className="search" onSubmit={find}><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Name, public ID, city, or email"/><button>Search</button></form>{found.length===0?<p className="empty">Search for eligible partners.</p>:<div className="cards">{found.map(x=><article key={x.id}><span>{x.publicCreatorId??x.publicMerchantId}</span><strong>{x.displayName??x.tradingName}</strong><p>{x.email??x.city}</p><button onClick={()=>add(x)}>{merchant?'Add creator':'Request to promote'}</button></article>)}</div>}</section>:<section><div className="title"><h2>{merchant?'Creator partnerships':'My merchants'}</h2><select value={status} onChange={e=>setStatus(e.target.value)}>{['All','Pending','Approved','Rejected','Suspended','Revoked','Expired','Blocked'].map(x=><option key={x}>{x}</option>)}</select></div>{!items?<p>Loading…</p>:items.length===0?<p className="empty">No partnerships found.</p>:<div className="cards">{items.map(x=><article key={x.id}><span>{x.status}</span><strong>{merchant?x.creatorName:x.merchantName}</strong><p>{new Date(x.requestedAtUtc).toLocaleDateString()}</p><div>{merchant&&x.status==='Pending'&&<><button onClick={()=>action(x,'approve')}>Approve</button><button className="danger" onClick={()=>action(x,'reject')}>Reject</button></>}{merchant&&x.status==='Approved'&&<><button onClick={()=>action(x,'suspend')}>Suspend</button><button className="danger" onClick={()=>action(x,'revoke')}>Revoke</button><button className="danger" onClick={()=>action(x,'block')}>Block</button></>}{merchant&&['Suspended','Blocked'].includes(x.status)&&<button onClick={()=>action(x,'reactivate')}>Reactivate</button>}{!merchant&&x.status==='Pending'&&<button className="danger" onClick={()=>action(x,'withdraw')}>Withdraw</button>}{!merchant&&x.status==='Approved'&&<button className="danger" onClick={()=>action(x,'stop-promoting')}>Stop promoting</button>}</div></article>)}</div>}</section>}</>}
